fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
psycopg2-binary>=2.9.9
pydantic>=2.5.0

# Async HTTP client. Used at RUNTIME by app/riot_client.py to call the Riot
# Games API (account-v1 + summoner-v4) for the player search feature, and also
# by the FastAPI TestClient transport in the unit suite.
httpx>=0.27.0

# Phase 4: Prometheus monitoring. Instrumentation is fail-safe — if these are
# missing the API and tests still run (the /metrics route degrades to a stub).
prometheus-fastapi-instrumentator>=6.1.0
prometheus-client>=0.19.0

# Test dependencies (Phase-1 unit suite; runs without Docker/DB)
pytest>=8.0.0
