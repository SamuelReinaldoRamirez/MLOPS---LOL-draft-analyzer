# Frontend environment — copy to `.env.local` and adjust if needed.
#
# Base URL of the LoL Draft Predictor API. Exposed to the browser (NEXT_PUBLIC_)
# because the prediction request is made client-side from the draft board.
# Defaults to the local docker-compose API if unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
