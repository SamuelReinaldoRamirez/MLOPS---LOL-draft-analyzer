pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
psycopg2-binary>=2.9.9
pyarrow>=14.0.0
requests>=2.31.0

# Phase 2: experiment tracking + model registry (client).
# Training degrades to a NO-OP if MLFLOW_TRACKING_URI is unset/unreachable.
mlflow>=2.10.0,<3.0.0

# Phase 4: data-drift detection. Imported lazily inside drift_report.py so the
# module/tests stay importable even if evidently is absent in a given env.
evidently>=0.4.0

# Phase 2 — DVC pipeline params loading. PyYAML lets train_all_models.py
# overlay `params.yaml` when present; the import is lazy so training stays
# runnable without it. The `dvc` CLI itself is only needed on the host (to
# drive `dvc repro / push / pull`) — see the root `make dvc-install` target.
PyYAML>=6.0

# Test dependencies (Phase-1 unit suite; runs without Docker/DB)
pytest>=8.0.0
